<Window x:Class="EdgeManager.Gui.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:views="clr-namespace:EdgeManager.Gui.Views"
        xmlns:viewModels="clr-namespace:EdgeManager.Gui.ViewModels"
        mc:Ignorable="d"
        Title="EdgeManager"
        WindowStartupLocation="CenterScreen"
        AllowDrop="True"
        MinWidth="1024" MinHeight="800"
        d:DesignHeight="450" d:DesignWidth="800"
        d:DataContext="{d:DesignInstance viewModels:DesignMainWindowViewModel, IsDesignTimeCreatable=True}">

    <Grid>
       <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <views:ConnectionCabView Grid.Row="0" DataContext="{Binding ConnectionCabViewModel}" />

        <views:TabView Grid.Row="1" />


    </Grid>
</Window>
