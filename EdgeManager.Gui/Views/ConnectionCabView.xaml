<UserControl x:Class="EdgeManager.Gui.Views.ConnectionCabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EdgeManager.Gui.Views"
             xmlns:viewModels="clr-namespace:EdgeManager.Gui.ViewModels"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance viewModels:DesignConnectionCabViewModel, IsDesignTimeCreatable=True}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto"  />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0" Grid.Column="0" Content="IoT Hub:" VerticalAlignment="Center" HorizontalAlignment="Right" />
        <ComboBox Grid.Row="0" Grid.Column="1" ItemsSource="{Binding IotHubInfos}" SelectedItem="{Binding SelectedIotHubInfo}" DisplayMemberPath="Name" />

        <Label Grid.Row="1" Grid.Column="0" Content="IoT Edge:" VerticalAlignment="Center" HorizontalAlignment="Right" />
        <ComboBox Grid.Row="1" Grid.Column="1" />

        <Label Grid.Row="2" Grid.Column="2" Content="Verbindungsstatus:" VerticalAlignment="Center" HorizontalAlignment="Right" />
        <Label Grid.Row="2" Grid.Column="3" />
        
        
        <Button Grid.Row="2" Grid.Column="0" Content="Connect" Margin="5"></Button>
        <Button Grid.Row="2" Grid.Column="1" Content="Disconnect" Margin="5"></Button>

    </Grid>
</UserControl>
